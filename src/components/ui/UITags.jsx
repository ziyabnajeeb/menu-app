import tw, { css, styled } from 'twin.macro';

export const Grid = tw.div`grid`;
export const Flex = tw.div`flex flex-1 flex-wrap`;
export const Container = tw.section`container mx-auto`;
export const Section = tw.section``;
export const SectionHeading = tw.section``;
